#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    vector<int>v1;
    vector<int>v2;
    for(int i=0;i<n;i++)
    {
        int p;
        cin>>p;
        v1.push_back(p);
    }
    for(int J=0;J<n;J++)
    {
        int q;
        cin>>q;
        v2.push_back(q);
    }
    sort(v1.rbegin(),v1.rend());
    sort(v2.rbegin(),v2.rend());
    for(int i=0;i<n;i++)
    {
        if(v1[i]<)
    }
}
